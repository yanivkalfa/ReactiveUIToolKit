import type { SxProps } from '@mui/material'

const root: SxProps = {
  display: 'flex',
  flexDirection: 'column',
  gap: 2,
}

const section: SxProps = {
  mt: 2,
}

const Styles = { root, section }

export default Styles


import type { SxProps } from '@mui/material'

const root: SxProps = { display: 'flex', justifyContent: 'space-between', borderTop: 1, borderColor: 'divider', mt: 4, pt: 2 }

const Styles = { root }

export default Styles

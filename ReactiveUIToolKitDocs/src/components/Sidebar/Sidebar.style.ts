import type { SxProps } from '@mui/material'

const root: SxProps = { width: 280, borderRight: 1, borderColor: 'divider', height: '100%', overflow: 'auto' }
const childItem: SxProps = { pl: 4 }

const Styles = { root, childItem }

export default Styles

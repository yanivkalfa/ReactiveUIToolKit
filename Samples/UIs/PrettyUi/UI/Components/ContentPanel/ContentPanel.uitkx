import { Body, Card, Title } from "./ContentPanel.style"
import "@UnityEngine.UIElements"

export VirtualNode ContentPanel(string title = "", string body = "") {
  return (
    <VisualElement style={Card}>
      <Label text={title} style={Title} />
      <Label text={body} style={Body} />
    </VisualElement>
  );
}

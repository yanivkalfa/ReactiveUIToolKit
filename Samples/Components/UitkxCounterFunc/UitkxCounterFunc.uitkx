@namespace ReactiveUITK.Samples.UITKXComponents

export component UitkxCounterFunc {
  var (count, setCount) = useState(0);

  return (
    <VisualElement>
      <Label text={$"Count: {count}"} />
      <Button text="+" onClick={_ => setCount(count + 1)} />
      <Button text="-" onClick={_ => setCount(count - 1)} />
    </VisualElement>
  );
}

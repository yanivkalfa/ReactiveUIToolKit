import { container } from "./SaaomeBla.style"
import { getSomething } from "./someThingElse.util"

export VirtualNode HmrTests() {
  var (count, setCount) = useState(0);

  return (
    <VisualElement style={container}>
      <Text text={$"Csount: {count}"} />
      <Button text="+" onClick={_ => setCount(getSomething())} />
    </VisualElement>
  );
}

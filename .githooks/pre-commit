#!/usr/bin/env bash
set -euo pipefail

# No-op: We commit Samples~ in the repo, and maintain a local dev copy of Samples (ignored by Git).
# This prevents massive staging diffs on pull/merge.
echo "[pre-commit] No action: using dev 'Samples' copy pattern (ignored by Git)."
exit 0

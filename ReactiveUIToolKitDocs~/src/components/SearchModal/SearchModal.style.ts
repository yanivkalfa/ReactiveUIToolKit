import type { SxProps } from '@mui/material'

const header: SxProps = { display: 'flex', alignItems: 'center', gap: 1, mb: 1 }
const inputPaper: SxProps = { p: 1, display: 'flex', alignItems: 'center', gap: 1, flex: 1 }
const noResults: SxProps = { p: 2 }
const content: SxProps = { pt: 1 }

const Styles = { header, inputPaper, noResults, content }

export default Styles

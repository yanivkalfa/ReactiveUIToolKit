import type { SxProps } from '@mui/material'

const root: SxProps = {
  display: 'flex',
  flexDirection: 'column',
  gap: 2,
}

const section: SxProps = {
  mt: 2,
}

const list: SxProps = {
  pl: 2,
}

const Styles = { root, section, list }

export default Styles

